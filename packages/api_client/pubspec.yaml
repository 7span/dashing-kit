name: api_client
description: "Used for calling API. Contains the Implementation using Dio by default"
version: 0.0.1
# homepage:

environment:
  sdk: ">=3.5.0-323.2.beta <4.0.0"
  flutter: ">=3.0.0 <4.0.0"

dependencies:
  # API Calling
  graphql: ^5.2.0-beta.7

  # For functional programming
  fpdart: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # Usage: dart run dependency_validator
  dependency_validator: ^4.1.0
